import { CorrectBgDirective } from './correct-bg.directive';

describe('CorrectBgDirective', () => {
  it('should create an instance', () => {
    const directive = new CorrectBgDirective();
    expect(directive).toBeTruthy();
  });
});
